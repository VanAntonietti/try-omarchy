# Agent instructions

## Agent skills

### Issue tracker

GitHub Issues in `VanAntonietti/try-omarchy`. Never infer the tracker from the `upstream` remote (`omacom/try-omarchy`). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root and ADRs in `docs/adr/`. See `docs/agents/domain.md`.
